version: "3"

services:

  mongodb:
    image: mongo

    environment:

      MONGO_INITDB_ROOT_USERNAME: admin

      MONGO_INITDB_ROOT_PASSWORD: MongoPass123

    ports:

      - "27017:27017"